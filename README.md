# homebrew-sabiql

`sabiql` is now available in Homebrew Core.

For new installations:

```sh
brew install sabiql
```

Existing installations from this tap migrate to Homebrew Core during `brew update`.
To update an existing installation and remove the old tap:

```sh
brew update
brew upgrade sabiql
brew untap riii111/sabiql
```
